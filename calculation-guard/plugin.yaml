name: calculation-guard
version: 0.1.0-beta.3
description: "Hermes Agent plugin that injects deterministic arithmetic, unit, and numerical plausibility context before local/small models answer."
author: Stefan Maier / Iris
provides_hooks:
  - pre_llm_call
provides_tools:
  - calculation_guard_status
  - calculation_guard_diagnostics
  - calculation_guard_config
